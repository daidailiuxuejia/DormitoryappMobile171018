<?xml version="1.0" encoding="utf-8"?>
<shape
  xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 填充色 --> 
    <solid android:color="#ffffffff"/>
    <!-- 圆角：android:radius角的弧度,值越大角越圆 -->
    <corners android:radius="2.0dip" />
    <!-- 边框：默认为实线；设置虚线可添加android:dashWidth(每条短线的宽度)和android:dashGap(短线间的间隙) -->
    <stroke android:width="1.0dip" android:color="#ffe7e7e7" />
    <!-- 内边距 -->
    <padding android:left="8.0dip" android:top="4.0dip" android:right="8.0dip" android:bottom="4.0dip" />
    
    <!-- 设置渐变使用gradient属性。android:startColor和android:endColor分别为起始和结束颜色，ndroid:angle是渐变角度，必须为45的整数倍。
另外渐变默认的模式为android:type="linear"，即线性渐变，可以指定渐变为径向渐变，android:type="radial"，径向渐变需要指定半径android:gradientRadius="50"。
     -->
</shape>
